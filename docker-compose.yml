services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "10000:10000"
    environment:
      - PORT=10000
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
